---
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';
import { useTranslations } from '../i18n/translations';
import type { Lang } from '../i18n/translations';

interface Props {
  title?: string;
  description?: string;
  lang?: string;
  ogImage?: string;
}

const {
  title = 'NEWFRAME, Custom Websites and Digital Direction',
  description = 'NEWFRAME is an independent design studio creating custom websites and digital experiences for founders, premium services and companies that want to look as good as they work.',
  lang = 'en',
  ogImage = '/og-image.jpg',
} = Astro.props;

const t = useTranslations((lang === 'pl' ? 'pl' : 'en') as Lang);

// Canonical URL
const canonicalUrl = new URL(Astro.url.pathname, Astro.site);

// Hreflang alternates
const pathWithoutLang = Astro.url.pathname.replace(/^\/(en|pl)/, '');
const enUrl = new URL(`/en${pathWithoutLang || '/'}`, Astro.site);
const plUrl = new URL(`/pl${pathWithoutLang || '/'}`, Astro.site);

// Absolute OG image URL
const ogImageUrl = new URL(ogImage, Astro.site);
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

    <!-- Canonical & hreflang -->
    <link rel="canonical" href={canonicalUrl.href} />
    <link rel="alternate" hreflang="en" href={enUrl.href} />
    <link rel="alternate" hreflang="pl" href={plUrl.href} />
    <link rel="alternate" hreflang="x-default" href={enUrl.href} />

    <!-- Theme color -->
    <meta name="theme-color" content="#f4f4f4" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl.href} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl.href} />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content={t.seo.ogImageAlt} />
    <meta property="og:locale" content={lang === 'pl' ? 'pl_PL' : 'en_US'} />
    <meta property="og:locale:alternate" content={lang === 'pl' ? 'en_US' : 'pl_PL'} />
    <meta property="og:site_name" content="New Frame Studio" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl.href} />
    <meta name="twitter:image:alt" content={t.seo.ogImageAlt} />

    <!-- Preload self-hosted fonts -->
    <link rel="preload" href="/fonts/GeneralSans-Medium.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/GeneralSans-Semibold.woff2" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="/fonts/GeneralSans-Bold.woff2" as="font" type="font/woff2" crossorigin />

    <!-- Preconnect to font servers -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <!-- DNS prefetch for third-party resources -->
    <link rel="dns-prefetch" href="https://images.unsplash.com" />
    <link rel="dns-prefetch" href="https://hoirqrkdgbmvpwutwuwj.supabase.co" />

    <!-- Google Fonts: Geist Sans + Instrument Serif (non-render-blocking) -->
    <link
      href="https://fonts.googleapis.com/css2?family=Geist:wght@400&family=Instrument+Serif:ital@0;1&display=swap"
      rel="stylesheet"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link
        href="https://fonts.googleapis.com/css2?family=Geist:wght@400&family=Instrument+Serif:ital@0;1&display=swap"
        rel="stylesheet"
      />
    </noscript>

    <!-- Hide preloader instantly if user has already seen it -->
    <script is:inline>
      if (localStorage.getItem('preloaded')) {
        document.documentElement.classList.add('preloader-done');
      }
    </script>
    <style is:inline>
      .preloader-done #preloader { display: none !important; }
    </style>

    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "Organization",
          "@id": `${Astro.site}#organization`,
          name: "New Frame Studio",
          url: Astro.site?.href,
          logo: new URL("/favicon.svg", Astro.site).href,
          founder: {
            "@type": "Person",
            name: "Jed Mejzner",
          },
          description: "Independent design studio creating custom websites and digital experiences for founders, premium services and companies that want to look as good as they work.",
        },
        {
          "@type": "WebSite",
          "@id": `${Astro.site}#website`,
          url: Astro.site?.href,
          name: "New Frame Studio",
          publisher: { "@id": `${Astro.site}#organization` },
          inLanguage: ["en", "pl"],
        },
        {
          "@type": "WebPage",
          "@id": canonicalUrl.href,
          url: canonicalUrl.href,
          name: title,
          description,
          isPartOf: { "@id": `${Astro.site}#website` },
          inLanguage: lang === "pl" ? "pl" : "en",
        },
      ],
    })} />

    <slot name="head" />

    <!-- View Transitions -->
    <ClientRouter />
  </head>
  <body>
    <slot />
  </body>
</html>
